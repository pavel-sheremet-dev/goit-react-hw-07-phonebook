import { FirstWord, LogoStyled } from './Logo.styled';

const Logo = () => {
  return (
    <LogoStyled href="#">
      <FirstWord>Phone</FirstWord>
      <span>book</span>
    </LogoStyled>
  );
};

export default Logo;
